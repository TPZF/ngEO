define(function () {

    // Define the QUnit module and lifecycle.
    QUnit.module("example");

    QUnit.test("example test", function () { 
        QUnit.equal(false, false);
    });

});